import { TextoMayusculaPipe } from './texto-mayuscula.pipe';

describe('TextoMayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new TextoMayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
